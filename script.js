import * as THREE from 'https://cdn.jsdelivr.net/npm/three@0.170.0/build/three.module.js';
const scene=new THREE.Scene(),camera=new THREE.PerspectiveCamera(55,innerWidth/innerHeight,.1,100);camera.position.z=7;const renderer=new THREE.WebGLRenderer({alpha:true,antialias:true});renderer.setPixelRatio(Math.min(devicePixelRatio,2));renderer.setSize(innerWidth,innerHeight);document.querySelector('#webgl').appendChild(renderer.domElement);
const geo=new THREE.IcosahedronGeometry(2.1,2),mat=new THREE.MeshBasicMaterial({color:0x9cff5b,wireframe:true,transparent:true,opacity:.045}),mesh=new THREE.Mesh(geo,mat);scene.add(mesh);
const pg=new THREE.BufferGeometry(),n=700,p=new Float32Array(n*3);for(let i=0;i<n;i++){const r=7*Math.random(),a=Math.random()*Math.PI*2,b=Math.acos(2*Math.random()-1);p[i*3]=r*Math.sin(b)*Math.cos(a);p[i*3+1]=r*Math.sin(b)*Math.sin(a);p[i*3+2]=r*Math.cos(b)}pg.setAttribute('position',new THREE.BufferAttribute(p,3));scene.add(new THREE.Points(pg,new THREE.PointsMaterial({color:0x9cff5b,size:.012,transparent:true,opacity:.4})));
let tx=0,ty=0,x=0,y=0;addEventListener('pointermove',e=>{tx=(e.clientX/innerWidth-.5)*.7;ty=(e.clientY/innerHeight-.5)*.5});addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)});function loop(){requestAnimationFrame(loop);x+=(tx-x)*.03;y+=(ty-y)*.03;mesh.rotation.y+=.0018+x*.002;mesh.rotation.x+=.0007+y*.002;renderer.render(scene,camera)}loop();
